core:
  f-cp [+]          Safe copy with full metadata preservation
                    (mtime/atime/ctime, ACLs, attributes) and clobber
                    protection.
  f-mv [+]          Safe move with full metadata preservation
                    (mtime/atime/ctime, ACLs, attributes) and clobber
                    protection.
  find              Cross-platform file search powered by fd
  fixpath           Fix mangled paths and optionally open, copy, or browse files
  links [lib]       Detect and display filesystem links (symlinks, junctions,
                    hardlinks, dazzlelinks)
  listall [+]       List filenames, directories, or specific files with
                    customizable sorting, collection, and output options
  rn                Rename files using regular expressions
  safedel [+][lib]  Safe file/directory deletion with link-aware
                    classification, metadata preservation, and recoverable
                    trash store

core:f:  (virtual kit 'f')
  cp  Safe copy with full metadata preservation (mtime/atime/ctime, ACLs,
      attributes) and clobber protection.
  ls  List filenames, directories, or specific files with customizable sorting,
      collection, and output options
  mv  Safe move with full metadata preservation (mtime/atime/ctime, ACLs,
      attributes) and clobber protection.
  rm  Safe file/directory deletion with link-aware classification, metadata
      preservation, and recoverable trash store

dazzletools:
  claude-cleanup [+]           Stage and commit Claude Code state files --
                               noise (transient) or user (configs, skills,
                               session logs)
  claude-recover-sesslogs [+]  Salvage the logger channels of sessions that
                               worked in a now-missing path -- rank sesslogs by
                               folder-affinity and recover them
  claude-session-metadata [+]  Catalog Claude Code sessions (lost or live) with
                               artifact cross-referencing, known-docs
                               discovery, and navigable symlinks
  claude-sesslog-datefix [+]   Fix timestamps on Claude Code session log
                               directories after git operations corrupt them
  claudeview [+]               Open Claude Code sessions in the history viewer
                               from the command line
  delete-nul                   Delete Windows NUL device files that get
                               accidentally created
  dos2unix                     Pure-Python cross-platform line ending converter
                               (dos2unix/unix2dos)
  extract-all                  Recursively extract archives (exe, zip, msi, 7z,
                               etc.) and locate files within by glob or regex
                               pattern
  fixuser [+]                  Diagnose and repair a broken Windows user
                               profile: detects temporary-profile / ProfileList
                               '.bak' state and stripped profile ACLs (the
                               takeown/icacls aftermath), then restores the
                               ProfileList entry and re-grants the user
                               FullControl loop-safely via safe-icacls
  git                          Git repo state inspector -- composition,
                               workspace, and form at a glance
  git-snapshot                 Lightweight named checkpoints for git working
                               state (save, diff, apply, restore)
  github                       Open GitHub project pages, issues, PRs, and
                               releases from any git repo
  md-rm-img [+]                Strip inline base64 image data from markdown
                               files; relink references to on-disk source via
                               local search + dz fixpath
  md-unwrap [+]                Unwrap hard-wrapped markdown paragraphs to one
                               line per paragraph; preserves
                               code/tables/lists/quotes/frontmatter.
                               Content-preserving, render-identical
  private-init                 Initialize private/ as a standalone versioned
                               git repo inside any project
  redact-msinfo [+]            Redact a Windows msinfo32 text export (PII
                               scrubbing + section stripping) for safe sharing;
                               pick sections with --include/--exclude
  safe-icacls [+]              Loop-safe passthrough wrapper around Windows
                               icacls: forwards any icacls args, but on /T
                               walks the tree itself and prunes reparse points
                               so self-referential junctions can't cause an
                               infinite recursion
  split                        Split text by separator with optional token
                               filtering
  srch-path                    Search the system PATH for executables and
                               commands

dazzletools:claude:  (virtual kit 'claude')
  cleanup           Stage and commit Claude Code state files -- noise
                    (transient) or user (configs, skills, session logs)
  recover-sesslogs  Salvage the logger channels of sessions that worked in a
                    now-missing path -- rank sesslogs by folder-affinity and
                    recover them
  session-metadata  Catalog Claude Code sessions (lost or live) with artifact
                    cross-referencing, known-docs discovery, and navigable
                    symlinks
  sesslog-datefix   Fix timestamps on Claude Code session log directories after
                    git operations corrupt them
  view              Open Claude Code sessions in the history viewer from the
                    command line

dazzletools:md:  (virtual kit 'md')
  rm-img  Strip inline base64 image data from markdown files; relink references
          to on-disk source via local search + dz fixpath
  unwrap  Unwrap hard-wrapped markdown paragraphs to one line per paragraph;
          preserves code/tables/lists/quotes/frontmatter. Content-preserving,
          render-identical

dazzletools:windows:  (virtual kit 'windows')
  fixusr         Diagnose and repair a broken Windows user profile: detects
                 temporary-profile / ProfileList '.bak' state and stripped
                 profile ACLs (the takeown/icacls aftermath), then restores the
                 ProfileList entry and re-grants the user FullControl
                 loop-safely via safe-icacls
  redact-msinfo  Redact a Windows msinfo32 text export (PII scrubbing + section
                 stripping) for safe sharing; pick sections with
                 --include/--exclude
  sicacls        Loop-safe passthrough wrapper around Windows icacls: forwards
                 any icacls args, but on /T walks the tree itself and prunes
                 reparse points so self-referential junctions can't cause an
                 infinite recursion

media:
  crossfade          Build a video from 2 images with a crossfade and an audio
                     track
  img2vid            Build a video from N images plus audio with sequential
                     transitions
  mp3me              Batch FLAC -> MP3/V0/V2/Ogg/AAC/ALAC transcoder with tag
                     copy (whatmp3)
  song-to-vid        Make a video per audio file from its embedded album art
  vid-preview-maker  Build a preview clip with a banner overlay and audio
                     fade-out
  vid2gif            High-quality video to GIF via ffmpeg two-pass palette +
                     gifsicle
  vidresize          AR-preserving video resize with metadata strip + audio
                     injection

wtf:core:
  locked     Why did my Windows PC lock? Diagnoses lock causes, identifies who
             logged in.
  restarted  Why did my Windows PC restart? One command, instant answers.

  [+] canonical has aliases (virtual-kit overlays and/or auto-realpath dedup of
      cross-aggregator embeddings) -- use 'dz info <name>' for alias details.
  [lib] constitutional primitive (engine in dazzlecmd_lib); absolute FQCN
        'dazzlecmd_lib:core:<name>'. Names show prefixless -- the absolute
        prepends this aggregator (e.g. 'dazzlecmd:core:<name>'); both resolve.
        'dz info <name>' shows it.

  36 tool(s) + 14 alias(es) (50 rows)
