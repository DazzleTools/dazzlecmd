# Disposable --fix laboratory. Creates an origin + clone pair entirely
# inside the scratchpad, makes the clone 1 behind, and leaves a config
# scoped to that directory. Nothing under C:\code is reachable.
