# v0.7.42 Test Checklist — git-repokit-common Subtree Integration

**Type:** Tool / Tooling integration (chore)
**Date:** 2026-05-14
**Branch:** `integrate-repokit-common` (merged origin/main; ready for fast-forward to main)
**Upstream pin:** `git-repokit-common@v0.2.4` (commit `09b6fa8`)

**Project-private** (not in public git history, for project maintainers):
- `2026-05-13__18-51-11__dev-workflow-process__git-repokit-common-integration.md` (dev-workflow design doc, SPCR + PUVM analysis)
- `2026-05-13__21-16-21__claude-plan__git-repokit-common-integration.md` (implementation plan)
- `commit_v0.7.42_issue24_repokit-common.txt` (commit message draft)
- `issue_24_2026.05.14_01.md` (issue progress comment draft)

## Prerequisites

- Python 3.8+ on PATH (`python --version` ≥ 3.8)
- bash on PATH (`bash --version`)
- `git` on PATH
- For PyPI-style behavior: optional `tomli` package if Python < 3.11 (`pip install tomli`)
- Network access for `bash scripts/update-common.sh --check` (fetches the `repokit-common` remote)

## Section 1 — Subtree integration is correct

- [ ] `ls scripts/` shows ~20 files including:
  - `sync-versions.py`, `update-common.sh`, `install-hooks.sh`, `update-version.sh`
  - `hooks/` subdirectory with `pre-commit`, `post-commit`, `pre-push`
  - `gh_issue_full.py`, `gh_sub_issues.py`, `search_sesslog.py`, `extract_tool_result.py`
  - `safe_move.sh`, `paths.sh`
  - `demo/` subdirectory with `build_demo.py`, `demo_render.py`, `vhs/`
  - `VERSION`, `README.md`, `CHANGELOG.md`, `LICENSE`, `CONTRIBUTING.md`, `TODO.md`, `docs/`
- [ ] `cat scripts/VERSION` prints `0.2.4`
- [ ] `git log --oneline --grep="Squashed 'scripts/'"` shows the subtree squash commit `b848d76`
- [ ] `git log --oneline --grep="as 'scripts'"` shows the subtree merge commit `1de0c06`

## Section 2 — sync-versions.py works (the canonical version manager)

- [ ] `python scripts/sync-versions.py --check` exits `0` and prints `All versions are in sync.`
- [ ] `python scripts/sync-versions.py --check --verbose` shows:
  - `Project root: <repo>`
  - `Source: src/dazzlecmd/_version.py`
  - `Current: 0.7.42 (PEP 440: 0.7.42)`
  - All `[OK]` rows
- [ ] `python scripts/sync-versions.py --dry-run` prints `All versions already in sync` and exits `0`
- [ ] `python scripts/sync-versions.py --help` shows the full usage with `--bump`, `--set`, `--phase`, `--demote`, `--check`, `--dry-run`, `--auto`, etc.
- [ ] `grep -E "^(MAJOR|MINOR|PATCH)" src/dazzlecmd/_version.py` shows:
  - `MAJOR = 0`
  - `MINOR = 7`
  - `PATCH = 42`

## Section 3 — `[tool.repokit-common]` config is wired correctly

- [ ] `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); print(d['tool']['repokit-common'])"` (or `tomli` on Python < 3.11) prints the config dict
- [ ] Config values match:
  - `version-source = "src/dazzlecmd/_version.py"`
  - `changelog = "CHANGELOG.md"`
  - `repo-url = "https://github.com/DazzleTools/dazzlecmd"`
  - `tag-prefix = "v"`
  - `tag-format = "pep440"`
  - `private-patterns` includes: `private/`, `convos/`, `logs/`, `test-runs/`, `test_runs/`, `.env`, `credentials/`, `secrets/`, `revisions/`

## Section 4 — Hooks installed and firing correctly

- [ ] `bash scripts/install-hooks.sh` succeeds (answer `y` if prompted to overwrite)
- [ ] Running `bash scripts/install-hooks.sh` again is idempotent (no errors; just confirms overwrite)
- [ ] `ls -la $(git rev-parse --git-common-dir)/hooks/` shows `pre-commit`, `post-commit`, `pre-push` (no `.sample` suffix on the live ones)
- [ ] **Hook fire-drill (real commit):** make a trivial whitespace edit, stage, commit. Verify:
  - Pre-commit prints `✓ Version updated for commit`
  - Post-commit prints `✓ Version updated with commit hash <SHA>`
  - `__version__` in `_version.py` reflects the new commit hash (`git diff HEAD~1 -- src/dazzlecmd/_version.py` shows the rebuild)
- [ ] **Private-content guard:** create a file matching a private pattern (e.g. `touch /tmp/test-pvt && cp /tmp/test-pvt convos/test-private.txt`), `git add convos/test-private.txt`. The pre-commit hook should BLOCK the commit on `main` (will pass on `private` / feature branches per branch-aware logic). Clean up with `dz safedel convos/test-private.txt`.
- [ ] **Large-file guard:** create a 12MB dummy (`dd if=/dev/zero of=big.bin bs=1M count=12`), `git add big.bin`. Pre-commit should reject with "File big.bin is too large". Clean up with `dz safedel big.bin`.

## Section 5 — update-common.sh (subtree sync)

- [ ] `bash scripts/update-common.sh --check` prints:
  - `Local version:  0.2.4`
  - `Upstream version: 0.2.4`
  - `Up to date.`
- [ ] `bash scripts/update-common.sh --help` shows usage with `pull`, `--check`, `--push`, `--help`

> **Do not exercise `pull` or `push` in this checklist** — those are operational verbs reserved for actual subtree updates / contributions.

## Section 6 — CHANGELOG scaffolding

- [ ] `## [0.7.42]` section near top of `CHANGELOG.md`, dated `2026-05-14`
- [ ] Compare-links block at the bottom contains:
  - `[Unreleased]: ...compare/v0.7.42...HEAD`
  - `[0.7.42]: ...compare/v0.7.41...v0.7.42`
  - `[0.7.41]: ...compare/v0.7.40...v0.7.41`
  - `[0.7.40]: ...compare/v0.7.39...v0.7.40`
  - `[0.7.39]: ...releases/tag/v0.7.39`
- [ ] `## [0.7.41]` and `## [0.7.40]` sections (from main's parallel commits) are also present, between `[0.7.42]` and `[0.7.39]`

## Section 7 — Runtime is unchanged

- [ ] `python -c "import sys; sys.path.insert(0,'src'); import dazzlecmd; print(dazzlecmd._version.get_version())"` prints `0.7.42_<branch>_<build>-<date>-<hash>`
- [ ] `python -c "import sys; sys.path.insert(0,'src'); import dazzlecmd; print(dazzlecmd._version.get_base_version())"` prints `0.7.42`
- [ ] `python -c "import sys; sys.path.insert(0,'src'); import dazzlecmd; print(dazzlecmd._version.get_display_version())"` prints `PREALPHA 0.7.42`
- [ ] `python -c "import sys; sys.path.insert(0,'src'); import dazzlecmd; print(dazzlecmd._version.PROJECT_PHASE)"` prints `prealpha`
- [ ] `pytest --ignore=projects/wtf -q` reports 0 failures (expected: ≥ 1212 passed, ≥ 21 skipped)
- [ ] `python -m dazzlecmd --version` prints `0.7.42_<branch>_<build>-<date>-<hash>` (matches `__version__`)
- [ ] `python -m dazzlecmd list` runs without exception
- [ ] `python -m dazzlecmd info` runs without exception

## Section 8 — Lockstep (CLAUDE.md Version Bump Checklist)

- [ ] `grep "^version" packages/dazzle-dz-alias/pyproject.toml` shows `version = "0.7.42"`
- [ ] `grep "dependencies" packages/dazzle-dz-alias/pyproject.toml` shows `["dazzlecmd>=0.7.42", "dazzlecmd-lib>=0.6.4"]`
- [ ] `grep "dazzlecmd-lib" pyproject.toml` shows `"dazzlecmd-lib>=0.6.4"` (aligned with the alias's pin)
- [ ] `grep "^version" packages/dazzlecmd-lib/pyproject.toml` shows `version = "0.6.4"` (independent cadence; bumped by main's v0.7.41)

## Section 9 — Old-vs-new audit (no functionality lost)

Confirms the old project-local `scripts/` content is fully covered by the new upstream subtree. Reference copy at `private/claude/revisions/scripts-pre-repokit-common/` (gitignored, local-only).

- [ ] Old `hooks/post-commit` → new is strict superset (sync-versions.py preferred, update-version.sh fallback)
- [ ] Old `hooks/pre-commit` → new is strict superset; **private-content regex is byte-identical**
- [ ] Old `hooks/pre-push` → new is significant upgrade (tag-only push fast path, auto-detects package dir, `compileall` handles src/ layout)
- [ ] Old `install-hooks.sh` → new auto-detects project name (more portable) and supports both sync-versions.py and update-version.sh
- [ ] Old `update-version.sh` → new is DEPRECATED in favor of sync-versions.py. **Known upstream bug:** new `update-version.sh` auto-detects package dir at one level deep, doesn't handle src/-layout projects. Tracked as Phase 8 upstream PR. Doesn't affect us functionally because pre-commit hook uses sync-versions.py as the primary path.

## Section 10 — Privacy discipline check

- [ ] `## [0.7.42]` CHANGELOG entry does NOT mention `private/claude/` or other project-private paths (filenames only if needed)
- [ ] `git log -1 --format=%B` (the v0.7.42 chore commit) does not reference any internal path
- [ ] `git log --oneline | grep -i "private/claude"` returns nothing
- [ ] `git log -p HEAD~5..HEAD -- 'private/*'` returns nothing (no private/ content was ever committed)

## Section 11 — Merge audit (for the upcoming fast-forward to main)

Run from `C:/code/dazzlecmd/github` (the main worktree) before fast-forwarding:

- [ ] `git fetch . integrate-repokit-common` succeeds
- [ ] `git log --oneline main..integrate-repokit-common` shows the 5 new commits (prep + 2 subtree + integration + merge)
- [ ] `git merge --ff-only integrate-repokit-common` would be a fast-forward (use `--dry-run` if such exists, or `git merge-base --is-ancestor main integrate-repokit-common && echo "FF possible"`)
- [ ] After fast-forwarding, `git log --oneline -8` on main shows the integration history in order

## Section 12 — Cross-shell sanity (manual, Windows)

- [ ] Open a fresh Windows `cmd.exe` terminal: `python scripts\sync-versions.py --check` works
- [ ] Open a fresh PowerShell terminal: `python scripts/sync-versions.py --check` works
- [ ] Git Bash / MSYS: `bash scripts/install-hooks.sh --help` doesn't error

## What mocks DON'T cover (why this checklist exists)

- Real `subprocess` invocation of `sync-versions.py` from the bash pre-commit hook in a real `git commit`
- Windows codepage 437 / 1252 handling for emoji output (`✓`, `❌`, `🔍`) from the hooks (cmd.exe may garble; PowerShell handles UTF-8)
- File atomicity of the `__version__` rewrite under real conditions (vs mocked file writes)
- bash-vs-Git-Bash variance on Windows
- The bare-repo + worktree geometry's effect on `git rev-parse --git-common-dir` and shared `.git/hooks/` resolution
- Hook idempotency when `install-hooks.sh` is run twice
- Whether `update-common.sh --check` actually fetches a remote and compares VERSION (cannot mock the GitHub network call)

## Planning lineage

Companion private-only docs (filenames only, project maintainers can find them in their working tree):

- `2026-05-13__18-51-11__dev-workflow-process__git-repokit-common-integration.md` (SPCR + PUVM design)
- `2026-05-13__21-16-21__claude-plan__git-repokit-common-integration.md` (implementation plan, all 9 phases)

Upstream pin: `git-repokit-common@v0.2.4` (commit `09b6fa8`).

Public GitHub issue: [#24 — Replace legacy scripts/ with git-repokit-common subtree](https://github.com/DazzleTools/dazzlecmd/issues/24)

## Phase 8 follow-up (separate timeline, NOT in this commit)

Each of these is an independent upstream PR to `git-repokit-common`. They land on the toolbox's `main`, then we pull via `bash scripts/update-common.sh` at our cadence:

- `feat(sync-versions): branch-aware-pep440 config` — upstream the branch → `.devN+branch` PEP 440 mapping currently in `_version.py:get_pip_version()`
- `feat(sync-versions): --project-phase flag` — write support for the `PROJECT_PHASE` field (currently read-only)
- `feat(hooks): private-patterns config drives pre-commit regex` — read `[tool.repokit-common] private-patterns` instead of hardcoding
- `fix(update-version.sh): src/-layout package detection` — make the deprecated fallback handle src/ layouts (mirror what pre-push does)
- `RFC + impl: multi-package lockstep config` — automate the manual `packages/dazzle-dz-alias/pyproject.toml` bump
- `feat(sync-versions): generic version-source for non-Python projects` — support C/C++ projects like `dazzle-locate32` (no `pyproject.toml`)

## Cleanup

- Remove any test-private files created in Section 4: `dz safedel <path>`
- Discard the hook fire-drill commit if you don't want to keep it: `git reset --hard HEAD~1` (only the throwaway commit; integration commits stay)
- The `__version__` left modified by the post-commit hook is expected; it auto-restages on the next real commit
