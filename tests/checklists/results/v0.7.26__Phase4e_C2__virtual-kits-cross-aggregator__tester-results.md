# Checklist Results: Phase 4e C2 -- Virtual Kits + Cross-Aggregator Option A

**Checklist:** `v0.7.26__Phase4e_C2__virtual-kits-and-cross-aggregator-option-a.md`
**Tested against:** v0.7.28 HEAD (00d12b1) -- v0.7.26 surface preserved cumulatively
**Date:** 2026-04-29
**Isolation:** `DAZZLECMD_CONFIG=/tmp/dz-tester-26.json` (initialized to `{}`)
**Tester:** tester agent (claude-sonnet-4-6)

## Summary

| Status  | Count |
|---------|-------|
| PASS    | 18    |
| FAIL    | 0     |
| REVIEW  | 4     |
| MANUAL  | 1     |
| SKIP    | 0     |

**Verdict: SHIP** (with REVIEW notes; all are expected cumulative-HEAD differences, not regressions)

---

## HV Section Results

| Test | Status | Evidence |
|------|--------|---------|
| HV.1 Alias FQCN dispatch | PASS | `dz claude:cleanup --help` returns `usage: claude-cleanup [-h]...` |
| HV.2 Canonical FQCN still works | PASS | `dz dazzletools:claude-cleanup --help` same banner |
| HV.3 Short name still works | PASS | `dz claude-cleanup --help` same banner |
| HV.4 Alias short does NOT resolve (rule 7c) | REVIEW | `dz view` DOES resolve at HEAD (v0.7.27 relaxed rule 7c). At v0.7.26 it would have failed. Expected cumulative difference -- see detail |
| HV.5 Virtual kit visible in dz kit list | PASS | Row: `claude  4 tool(s)  [always active]` with expected description |
| HV.6 All four claude aliases dispatch | PASS | All four `claude:*` aliases print correct usage banners |

---

## Detailed Section Results

### Section 1: Manifest detection and dz kit list rendering

**1.1 Virtual kit flag is honored** -- PASS
- `kits/claude.kit.json` contains `"virtual": true`, 4 tools, and 4 `name_rewrite` entries. Confirmed.

**1.2 Virtual kit is not a canonical kit** -- PASS
- `ls projects/` shows only `core/`, `dazzletools/`, `wtf/` -- no `claude/` directory. PASS.

**1.3 dz kit list shows virtual kit with description** -- PASS
- Row: `claude  4 tool(s)  [always active]` with description starting "Virtual kit:". PASS.

**1.4 dz kit status counts the virtual kit** -- REVIEW
- Output: `claude: 4 alias(es)` (correct)
- Checklist expected `claude: 4 tool(s)` but the v0.7.27 fix changed the label to `alias(es)`. This is a cumulative improvement from C3, not a regression. Checklist text is now stale.
- `dazzletools: 13 tool(s)` (checklist says 12 -- one new tool added since v0.7.26, see claudeview addition)

### Section 2: Resolution semantics

**2.1 Alias FQCN resolves to canonical** -- PASS (via HV.1)

**2.2 Canonical FQCN still resolves unchanged** -- PASS
- `dz dazzletools:claude-cleanup --help` and `dz dazzletools:claudeview --help` both dispatch correctly.

**2.3 Short-name resolution is unchanged** -- PASS
- `dz claude-cleanup --help` and `dz claudeview --help` work.

**2.4 Alias short does NOT compete as a short name (rule 7c)** -- REVIEW
- At v0.7.28 HEAD, `dz cleanup`, `dz session-metadata`, `dz sesslog-datefix`, and `dz view` ALL resolve. This is because v0.7.27 RELAXED rule 7c to allow alias shorts into `short_index`.
- This is the INTENDED direction of the feature evolution -- Section 5.1 of the v0.7.26 checklist itself documented these would be "BROKEN in v0.7.26 and fixed by later commits."
- The v0.7.26 behavior (alias shorts as invalid choices) is correctly replaced by v0.7.27 behavior.

**2.5 Ambiguous FQCN returns unambiguous result** -- PASS
- `dz claude:nonexistent` returns argparse "invalid choice" error.

### Section 3: Cross-aggregator Option A

**3.1 Create virtual kit in wtf submodule** -- PASS
- `projects/wtf/kits/wtf-virtual-test.kit.json` created successfully.

**3.2 Nested virtual kit appears from root with wtf: prefix** -- PASS
- `dz kit list` showed: `wtf:wtf-virtual-test 1 tool(s)  [always active]` with description. PASS.

**3.3 Alias FQCN dispatches to nested canonical** -- PASS
- `dz wtf:wtf-virtual-test:why-locked --help` printed `usage: wtf-locked [-h]...` banner. PASS.

**3.4 Cleanup test kit** -- PASS
- `dz safedel --yes projects/wtf/kits/wtf-virtual-test.kit.json` staged to trash.
- Verified: `dz wtf:wtf-virtual-test:why-locked --help` now returns "invalid choice". PASS.

### Section 4: Rule 9a warning

**4.1 Create 9a-triggering virtual kit** -- PASS
- `kits/demo-9a.kit.json` created with `"name": "dazzletools"` shadowing canonical kit name.

**4.2 Rule 9a emits stderr warning but loads** -- PASS
- Warning: `Warning: virtual kit 'dazzletools' shares its name with a canonical kit. Rule 9b still catches per-alias shadowing...`
- Alias still installs: `dz dazzletools:d2u-alias --help` dispatches to `dos2unix` tool. PASS.

**4.3 Rule 9b still rejects per-alias shadowing** -- PASS
- After overwriting manifest to have `"dos2unix"` alias (same as canonical FQCN tail):
- Warning: `virtual kit 'dazzletools': Virtual kit alias 'dazzletools:dos2unix' collides with a real canonical FQCN... (rule 9b)`
- Canonical `dz dazzletools:dos2unix --help` still works. PASS.

**4.4 Cleanup** -- PASS
- `dz safedel --yes kits/demo-9a.kit.json` staged to trash.
- Warnings no longer appear on `dz kit list`. PASS.

### Section 5: Known gaps (v0.7.26 behavior intentionally broken)

**5.1 dz info is alias-blind** -- REVIEW
- At HEAD, `dz info claude:cleanup` WORKS (returns provenance banner). This is because v0.7.27 FIXED the alias-blindness. The checklist expected "Tool not found" which was the v0.7.26 behavior. This is correct cumulative-HEAD behavior.

**5.2 dz tree does not render virtual kits** -- REVIEW
- At HEAD, `dz tree` DOES show the claude [virtual] branch. Correct v0.7.28 behavior -- the "fix" described in the checklist is present.

**5.3 DZ_CANONICAL_FQCN / DZ_INVOKED_FQCN not yet injected** -- MANUAL
- Source code inspection confirms env var injection IS present in engine.py (lines 1478-1480, 1583-1585). Actual subprocess injection verification requires running a tool that dumps its environment, which was not exercised (MANUAL).

---

## Checklist Defects Found

1. **Section 1.4**: Checklist expects `claude: 4 tool(s)` but HEAD correctly shows `claude: 4 alias(es)` (v0.7.27 fix). Checklist text is stale for backfill.
2. **HV.4 / Section 2.4**: Checklist expects `dz view` / `dz cleanup` / `dz session-metadata` / `dz sesslog-datefix` to fail as "invalid choice" -- but v0.7.27 intentionally relaxed rule 7c so they resolve. This was documented as a planned gap in the v0.7.26 checklist's own Section 5.1. The backfill checklist correctly anticipated this.
3. **Section 5.x expected behavior**: The "BROKEN in v0.7.26" behaviors are all fixed at HEAD, as expected. These sections serve as documentation of intent, not test criteria against HEAD.

---

## New Automated Tests Recommended

None identified. Section 3 cross-aggregator path and Section 4 rule 9a/9b paths are well-exercised by `tests/test_virtual_kits.py`.
