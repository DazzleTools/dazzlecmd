# Tester-unbounded results — v0.9.x checklists (overlay / restore / PowerShell links)

**Date:** 2026-06-11
**Against:** dazzlecmd v0.9.15 (`ecc1dc3` HEAD at run time; code at `b50920e`) / lib 0.8.17
**Agent:** tester-unbounded (sandbox + isolated config; no real project/kit/config touched; git status byte-identical to session start)

## Verdicts

| Checklist | Verdict |
|---|---|
| `v0.9.11__Phase2__overlay-as-groupable-fqcn-identity.md` | **SHIP** |
| `v0.9.13__Feature__mode-restore-origins-reversibility.md` | **SHIP** |
| `v0.9.15__Refactor__powershell-link-creation.md` | **SHIP** |

**Automated:** 95 test invocations (overlapping suites), **0 failures**. Byte-gate **10/10 OK**. No FAILs anywhere.

## What was verified automatically (PASS)

- **Overlay:** absolute home dispatch (`dz dazzlecmd_lib:core:safedel --help`), overlay provenance banner in `dz info <home>`, no banner for `dz info core:safedel`, `links [lib]` without `[+]`, overlay alias absent from `--show alias`, native self-prefix dispatch, non-constitutional lib-prefix → not found, `links` home parity. test_constitutional_identity + test_states + byte-gate green.
- **Restore:** the EMBEDDED switch→restore round-trip (real junction + isolated trash), origin record/clear, all refusal paths (no origin / not-in-dev / missing-backup leaves symlink), `--dry-run` no-op, EMBEDDED-reversible reclassification. 13/13 test_mode_origins + states green.
- **PowerShell links:** round-trip (symlink path here), spaced-path quoting, occupied-target refusal, the mode-swap integration round-trip, and the `undo()` message uses `{self.command}` (no literal `'dz mode restore'`). 3/3 test_core_links + 76/76 mode/states green.

## MANUAL / human-only items still open (no code blocker)

1. **v0.9.15 HV.2 — junction fallback on a Developer-Mode-OFF / non-elevated box (highest priority).** This box has Dev Mode ON, so `create_link` returned `symlink`; the junction fallback path was not exercised. This is the exact CI gap the checklist exists to close — needs a person on a non-Dev-Mode box.
2. **v0.9.13 HV.1–HV.6 real-terminal feel** with a hand-built sandbox tool (dirty-dev warning wording, junction creation visuals). Automated round-trip covers the mechanism; the terminal feel is human-only.
3. **v0.9.13 §1.2 SUBMODULE re-clone** — needs a safe sandbox submodule (skipped per checklist).
4. **v0.9.11** overlay banner dim/color rendering + `[lib]`/`[+]` glance-distinction in a real terminal.
5. **v0.9.15** real-terminal message when BOTH SymbolicLink and Junction fail; `powershell` absent from PATH; cross-volume links.

## Notes folded back into the checklists

- v0.9.11 HV.4: clarified that a local `f` virtual kit (`f:rm → core:safedel`, possibly `always_active`) legitimately adds `[+]` to `safedel` even with `active_kits: []`; a clean public checkout without that kit shows only `[lib]`. (The agent observed `[+]` because the run happened in the working tree that carries the parked `kits/f.kit.json`.)
- v0.9.13 companion-test count corrected 16 → 13 (8 origins + 5 restore).

## Safety confirmation (the user's explicit requirement)

No real tool/kit/project/config modified. All config-dependent `dz` commands ran under an isolated `DAZZLECMD_CONFIG` temp file. Temp link-test dirs cleaned via `dz safedel --yes`. No `git add`/commit/push. `git status` after the run is byte-for-byte identical to session start (same WIP batch, no new files).
