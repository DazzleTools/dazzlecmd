{
  "test_command": ["python", "-m", "pytest", "-q",
                   "src/dazzlecmd/projects/dazzletools/github/tests/test_github_resolution.py"],
  "mutations": [
    {"id": "M1", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "    exact = [r for r in found if r.get(\"name\", \"\").lower() == name_lower]",
     "mutated": "    exact = [r for r in found if r.get(\"name\", \"\") == name_lower]",
     "note": "Drop .lower() on the repo name so exact-match detection becomes case-sensitive."},
    {"id": "M2", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "    rest.sort(key=lambda r: len(r.get(\"name\", r[\"fullName\"])))\n    candidates = exact + rest",
     "mutated": "    rest.sort(key=lambda r: len(r.get(\"name\", r[\"fullName\"])))\n    candidates = rest + exact",
     "note": "Swap exact-first ordering so a single exact match auto-selects a non-exact candidate at index 0."},
    {"id": "M3", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "    if len(candidates) == 1 or len(exact) == 1:",
     "mutated": "    if len(candidates) == 1 and len(exact) == 1:",
     "note": "or->and: a single exact match among many no longer auto-selects; falls to prompt."},
    {"id": "M4", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "        idx = int(choice) - 1\n        if 0 <= idx < len(candidates):",
     "mutated": "        idx = int(choice)\n        if 0 <= idx < len(candidates):",
     "note": "Off-by-one: drop the -1 so prompt choice N selects candidate N+1."},
    {"id": "M5", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "    if \"/\" in name:\n        return name.strip()",
     "mutated": "    if \"/\" in name:\n        return name",
     "note": "Dropped normalization: OWNER/REPO slugs pass through with surrounding whitespace intact."},
    {"id": "M6", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "    if cached_repos and not refresh:",
     "mutated": "    if cached_repos or not refresh:",
     "note": "and->or: consult the (possibly empty) cache path even when refresh was requested or no cache exists."},
    {"id": "M7", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "        if is_fresh:\n            print(f\"No repo matching '{name}' in your orgs.\", file=sys.stderr)\n            return None",
     "mutated": "        if not is_fresh:\n            print(f\"No repo matching '{name}' in your orgs.\", file=sys.stderr)\n            return None",
     "note": "Flip freshness guard: fresh-cache miss now hits the API, stale-cache miss gives up without refreshing."},
    {"id": "M8", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "    if not target or \"#\" not in target:\n        return None",
     "mutated": "    if \"#\" not in target:\n        return None",
     "note": "Remove the None/empty guard so _split_issue_ref crashes with TypeError when target is None."},
    {"id": "M9", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "    return repo_part.strip(), issue_part.strip()",
     "mutated": "    return issue_part.strip(), repo_part.strip()",
     "note": "Swap tuple elements: repo and issue halves of '<repo>#<issue>' come back reversed."},
    {"id": "M10", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "    if \"#\" in target:\n        return False\n    return not target.isdigit()",
     "mutated": "    if \"#\" in target:\n        return False\n    return target.isdigit()",
     "note": "Drop the not: all-digit targets become repo names and word targets stop being repo names."},
    {"id": "M11", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "        repo_part, issue_part = issue_ref\n        if repo_part:",
     "mutated": "        repo_part, issue_part = issue_ref\n        if repo_part is not None:",
     "note": "Truthiness->is not None: bare '#N' (empty repo_part) now tries cross-repo resolution instead of the current project."},
    {"id": "M12", "file": "src/dazzlecmd/projects/dazzletools/github/github.py",
     "original": "    if refresh and args.target is None:",
     "mutated": "    if refresh and not args.target:",
     "note": "is None->falsy: with --refresh, an empty issue-ref remainder ('#') is mistaken for no target and only refreshes the cache."}
  ]
}
