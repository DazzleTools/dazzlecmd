name: Publish to PyPI

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type YES to confirm manual publish'
        required: true

permissions:
  id-token: write
  contents: read

jobs:
  build:
    name: Build packages
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive  # materialize submodule tools (e.g. core/listall) so package-data ships them

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.12'

    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine

    - name: Build main package (dazzlecmd)
      run: python -m build --outdir dist/main/

    - name: Build alias package (dazzle-dz)
      run: python -m build packages/dazzle-dz-alias/ --outdir dist/alias/

    - name: Verify no private/ or wtf content leaked into the artifacts
      run: |
        python scripts/check_dist_no_leak.py dist/main/
        python scripts/check_dist_no_leak.py dist/alias/

    - name: Check packages
      run: |
        twine check dist/main/*
        twine check dist/alias/*

    - uses: actions/upload-artifact@v7
      with:
        name: dist-main
        path: dist/main/

    - uses: actions/upload-artifact@v7
      with:
        name: dist-alias
        path: dist/alias/

  publish-main:
    name: Publish dazzlecmd
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/dazzlecmd

    permissions:
      id-token: write

    steps:
    - uses: actions/download-artifact@v8
      with:
        name: dist-main
        path: dist/

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        # Tag-push publishes must be idempotent: the alias package version
        # moves slower than the main package, so re-publishing an existing
        # file is expected, not an error (400 File already exists killed the
        # v0.12.4-alpha alias job -- the workflow's second-ever run).
        skip-existing: true

  publish-alias:
    name: Publish dazzle-dz
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/dazzle-dz

    permissions:
      id-token: write

    steps:
    - uses: actions/download-artifact@v8
      with:
        name: dist-alias
        path: dist/

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        # Tag-push publishes must be idempotent: the alias package version
        # moves slower than the main package, so re-publishing an existing
        # file is expected, not an error (400 File already exists killed the
        # v0.12.4-alpha alias job -- the workflow's second-ever run).
        skip-existing: true
