# Contributing to {name}

(Fallback-minimal stub placed by `--with template` because the real
git-repokit-template was unavailable. Expand to taste.)

- Open an issue describing the change before large PRs.
- Run the tests (`python -m pytest tests -q`) before submitting.
- Keep tools self-contained under {tools_dir}/<namespace>/<tool>/ with a
  {manifest} manifest.
