/* {name} -- {description} */

#include <stdio.h>

int main(int argc, char **argv) {
    printf("{name}: not yet implemented\n");
    if (argc > 1) {
        printf("Arguments:");
        for (int i = 1; i < argc; ++i) {
            printf(" %s", argv[i]);
        }
        printf("\n");
    }
    return 0;
}
