"""Version information for {name}."""

__version__ = "0.1.0"
__app_name__ = "{name}"

# Shown in the CLI banner; adjust the phase label as the project matures.
DISPLAY_VERSION = f"PREALPHA {__version__}"
